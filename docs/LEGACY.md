# Legacy site (deprecated)

This folder contains the previous JSON-driven site. It is **no longer deployed**.

The live site is built from `src/` with Eleventy into `_site/` and deployed via GitHub Actions.

This folder is kept temporarily for reference during migration and can be deleted once the new site is verified in production.
